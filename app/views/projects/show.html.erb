<head>
  <script src="https://kit.fontawesome.com/ec017e33af.js" crossorigin="anonymous"></script>
</head>
<body>
<!-- banner part start-->
<section class="banner_part">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-12">
                <div class="banner_slider owl-carousel">
                    <div class="single_banner_slider" style="max-height: 100px">
                        <div class="row">
                            <div class="col-lg-12 col-md-12">
                                <div class="banner_text">
                                    <div class="banner_text_iner">
                                        
                                    </div>
                                </div>
                            </div>

                        </div>
                    </div>
                </div>
                <div class="slider-counter"></div>
            </div>
        </div>
    </div>
</section>
<!-- banner part start-->

  <!--================Single Product Area =================-->
  <div class="product_image_area section_padding">
    <div class="container">
      <div class="row s_product_inner justify-content-between">
        <div class="col-lg-7 col-xl-7">
          <div class="product_slider_img">
            <div id="vertical">
              <div data-thumb="img/product/single-product/product_1.png">
                <% if @project.picture.attached? %>
                        <%= image_tag @project.picture, alt: 'picture', height: '100%', width: '100%'%> 
                      <%else%>
                      <p> no picture </p>
                      <%end%>
              </div>
              
            </div>
          </div>
        </div>
        <div class="col-lg-5 col-xl-4">
          <div>
            <h3><%= @project.title %></h3>
            <hr>
            <p>Type d'action : <b> Ecologique </b></p> 
            <hr>                                                      
            <h4> Ressources Requises</h4> <br> 
            <p>Besoins Humains : <b> <%= @project.number_of_participants %> personnes </b> <br> Besoins financier : <b> <%= @project.amount_needed %> $ </b><br> Financement récolté : <b> <%= @project.amount_state %> $ </b></p>
            <hr>
            <p>Fin des participations le <b><%= @project.deadline %></b></p>
            <hr>
            <p> <%= @project.description %> </p>
            <p> https://www.leetchi.com/fr/c/lW3bmkQl </p>
              </li>
            </ul>
            <% if current_user != @project.architect %>
              <div class="card_area d-flex justify-content-between align-items-center">
                <% if @project.number_of_participants <= @project.attendances.length %>
                  <p>Le nombre de participants maximum a été atteint.</p>
                <% elsif @project.participants.include?(current_user) %>
                  <p>Vous participez déjà à ce projet !</p>
                <% else %>
                  <%= button_to "Participer au projet", attendances_path, data: { confirm: "Voulez-vous rejoindre le projet ? Un email avec les coordonnées de l'organisateur vous sera alors envoyé." , disable_with: 'Déjà rejoint' }, params: { project_id: @project.id }, class: "btn_3" %>
                <% end %>
                <div class="like_us"> <i class="fas fa-heart"></i> 
                <%= link_to "Like", like_projects_path(project_id:@project.id), method: :post, :class=> 'btn_1' %>
                </div>
                  <%= link_to "Faire un don", new_charge_path( :project_id => @project.id), class: 'btn_2' %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
  <!--================End Single Product Area =================-->

  


</body>
